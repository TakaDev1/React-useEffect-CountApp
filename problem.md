## **問題2：状態変更に応じた副作用**

カウントを表示するボタンを作成し、カウントが更新されるたびに
「カウントが更新されました: [count]」とコンソールに表示せよ。

---

### **条件**

(1) `count` を `useState` で管理すること

(2) `useEffect` の依存配列に `count` を設定すること

(3) **CSS Modulesを使用して、ボタンを青背景・白文字にすること**

(4) CSSファイルは `Button.module.css` として作成すること

(5) `className` にはCSS Modulesで定義したクラスを使用すること
